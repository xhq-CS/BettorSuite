kind = "web"
previewPath = "/"
title = "PropEdge"
version = "1.0.0"
id = "artifacts/propedge"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22467

[services.development]
run = "pnpm --filter @workspace/propedge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/propedge", "run", "build" ]
serve = "static"
publicDir = "artifacts/propedge/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22467"
BASE_PATH = "/"
