import { useQuery } from "@tanstack/react-query";
import { Trophy } from "lucide-react";
import { api } from "@/lib/api";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
type Entry={rank:number;userId:number;username:string;totalBets:number;wins:number;winRate:number;roi:number;totalProfit:number};
export default function Leaderboard(){const board=useQuery({queryKey:["leaderboard"],queryFn:()=>api<Entry[]>("/leaderboard")});return <div className="max-w-5xl mx-auto space-y-6"><div><h1 className="text-3xl font-display font-bold">Leaderboard</h1><p className="text-sm text-muted-foreground mt-1">Community rankings based on settled tracked bets.</p></div><Card><CardHeader><CardTitle className="flex items-center gap-2"><Trophy className="w-5 h-5 text-amber-500"/>Global rankings</CardTitle></CardHeader><CardContent className="p-0"><Table><TableHeader><TableRow><TableHead className="pl-6">Rank</TableHead><TableHead>Bettor</TableHead><TableHead className="text-right">Record</TableHead><TableHead className="text-right">Win rate</TableHead><TableHead className="text-right">ROI</TableHead><TableHead className="text-right pr-6">Profit</TableHead></TableRow></TableHeader><TableBody>{board.data?.map(entry=><TableRow key={entry.userId}><TableCell className="pl-6 font-mono">#{entry.rank}</TableCell><TableCell className="font-medium">@{entry.username}</TableCell><TableCell className="text-right">{entry.wins}W · {entry.totalBets-entry.wins}L</TableCell><TableCell className="text-right">{(entry.winRate*100).toFixed(1)}%</TableCell><TableCell className="text-right">{entry.roi>=0?"+":""}{(entry.roi*100).toFixed(1)}%</TableCell><TableCell className="text-right pr-6">{entry.totalProfit>=0?"+":""}${entry.totalProfit.toFixed(2)}</TableCell></TableRow>)}{!board.isLoading&&!board.data?.length&&<TableRow><TableCell colSpan={6} className="text-center py-16 text-muted-foreground">No rankings yet. Settled bets will appear here.</TableCell></TableRow>}</TableBody></Table></CardContent></Card></div>}
