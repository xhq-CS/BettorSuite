{
  "name": "workspace",
  "version": "2.5.1",
  "description": "BettorSuite sportsbook journal, mock betting workspace, and social betting community.",
  "packageManager": "pnpm@11.15.1",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/bettorsuite dev",
    "dev:app": "pnpm --filter @workspace/bettorsuite dev",
    "preview": "pnpm --filter @workspace/bettorsuite serve",
    "vercel-build": "pnpm --filter @workspace/api-server build && pnpm --filter @workspace/bettorsuite build",
    "db:push": "pnpm --filter @workspace/db push",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}
